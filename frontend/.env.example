# Required in production for signing local auth tokens.
# Generate with: openssl rand -base64 32
APP_JWT_SECRET=replace-with-a-long-random-secret

# Optional compatibility fallback for older backend-style env names.
JWT_SECRET_KEY=replace-with-a-long-random-secret

# Built-in auth token expiry is fixed at one hour in the Next.js API routes.
